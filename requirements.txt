# Web framework
fastapi
uvicorn[standard]
scalar-fastapi

# Database
sqlalchemy[asyncio]
asyncpg
alembic

# Cache / broker
redis[hiredis]
celery

# HTTP client (for external subscription fetching)
aiohttp

# Config / validation
pydantic
pydantic-settings

prometheus_client>=0.25.0
opentelemetry-api>=1.42.1
opentelemetry-sdk>=1.42.1
